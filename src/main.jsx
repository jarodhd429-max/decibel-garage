import React from "react";
import ReactDOM from "react-dom/client";
import DecibelGarage from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DecibelGarage />
  </React.StrictMode>
);
